# Street Issue Reporter

This web application is for use by residents to report concerns or issues within their neighborhood or municipality.

# Installation

To use the application for local development, simply clone the repo, and run `npm install`.

Then run `npm run dev` and go to <localhost:3000> in your browser.

